Walk Meetup pages:
- /walks: list and filter
- /walks/new: host form to publish
- /walks/[id]: details, join, ticket QR, ICS
- /walks/[id]/host: host dashboard with QR check-in (BarcodeDetector) and manual toggles
- /admin/walks: admin moderation and bulk cancellation
Storage: localStorage ("dogmates-walks"). Not for production.
