name: Build and deploy Node.js app to Azure Web App - app-002-gen10-step3-2-node-oshima13

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      - name: Install deps
        run: npm ci

      - name: Build (standalone)
        run: |
          set -e
          npm run build
          # クリーン
          rm -rf deploy
          mkdir -p deploy
          # ドットディレクトリ(.nextなど)を含めてstandalone成果物を丸ごとコピー
          cp -R .next/standalone/. deploy/
          # static資産を配置 (.next/static がまだ無いので作成)
            mkdir -p deploy/.next/static
          cp -R .next/static/. deploy/.next/static/
          # public (存在する場合のみ)
          if [ -d public ]; then cp -R public deploy/public; fi
          # 追加必要ファイル
          cp package.json deploy/
          cp server.js deploy/
          if [ -f web.config ]; then cp web.config deploy/; fi
          echo '--- deploy 内容確認 (先頭50件) ---'
          find deploy -maxdepth 4 -type f | head -n 50

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: app-002-gen10-step3-2-node-oshima13
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_APP_002_GEN10_STEP3_2_NODE_OSHIMA13 }}
          package: ./deploy
