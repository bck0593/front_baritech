# Azure Database for MySQL接続設定
DB_USER=tech0gen10student
DB_PASSWORD=vY7JZNfU
DB_HOST=rdbs-002-gen10-step3-2-oshima13.mysql.database.azure.com
DB_PORT=3306
DB_NAME=dog_care_db

# JWT認証設定
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# アプリケーション設定
DEBUG=True
HOST=0.0.0.0
PORT=8000

# ファイルアップロード設定
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760  # 10MB

# SSL証明書ファイル名（プロジェクトルートに配置）
SSL_CERT_FILENAME=DigiCertGlobalRootCA.crt (4).pem

# CORS設定（フロントエンドからのアクセスを許可）
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001
